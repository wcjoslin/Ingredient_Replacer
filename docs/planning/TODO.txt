# TODO

- Implement caching for USDA FoodData Central API results to reduce API calls and improve speed.
- Add error handling and retry logic for API requests.
- Support batch queries for multiple ingredients.
- Add support for other nutrition databases if needed.
- Integrate per-ingredient nutrition checks into ingredient swap logic.
- Add unit tests for nutrition lookup and swap logic.
- Investigate integration between USDA and Open Food Facts APIs. Explore approaches such as ingredient normalization, synonym mapping, fallback logic, and others to improve matching and data enrichment.

## Long Term Enhancement
- Add support for religious dietary restrictions (kosher, halal, etc.) using specialized databases, ontologies, or rule-based logic.

## Embedding Expansion & Quality
- Identify ingredients with low sentence count in their embeddings.
- For those ingredients, expand the sentence set to at least 100 recipe-relevant sentences.
- Remove sentences sourced from ingredient lists (use only recipe instructions).
- Re-generate embeddings after expansion for improved swap accuracy.

## Suggestion Logic
- Integrate FoodBERT similarity scores to find reasonable substitutes.
- Use USDA nutrition data to evaluate the impact of swaps on calories, carbs, protein, fat, etc.
- Combine ingredient metadata from Open Food Facts and Suggestic where available.
- Apply culinary knowledge and rules to avoid swaps that are technically similar but not reasonable in recipes (e.g., sugar for salt).
- Ensure the workflow checks both semantic similarity and nutrition impact before making a suggestion.
